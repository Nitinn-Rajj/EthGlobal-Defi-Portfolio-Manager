# Required environment variables for the Agentverse proxy server

# Agentverse API configuration
AGENTVERSE_API_KEY=your_agentverse_api_key_here
# Full URL to Agentverse invoke endpoint
# Option 1: URL with {agentId} placeholder - will replace with actual agentId
# AGENTVERSE_INVOKE_URL=https://agentverse.ai/v1/agents/{agentId}/invoke
# Option 2: Base URL - will append /{agentId}/invoke automatically  
AGENTVERSE_INVOKE_URL=https://agentverse.ai/agents

# CORS configuration
FRONTEND_ORIGIN=http://localhost:5173

# Server configuration
PORT=3001

# Rate limiting (optional, defaults to 60)
RATE_LIMIT_REQS_PER_MIN=60

# Streaming support (optional, defaults to false)
ENABLE_STREAMING=false